import 'package:flutter/material.dart';

class SideNavgation extends StatelessWidget {
  const SideNavgation({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
